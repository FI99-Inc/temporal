{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local synthetic Horizon window; no filesystem, network, or shell plugin permissions.",
  "windows": ["main"],
  "permissions": []
}
